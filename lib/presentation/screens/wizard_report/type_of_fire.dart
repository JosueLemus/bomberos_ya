import 'package:flutter/material.dart';

class TypeOfFire extends StatelessWidget {
  const TypeOfFire({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Selecciona el tipo de incendio')
      ],
    );
  }
}
